Read-only Kubernetes tools (`get_pod_status`, `describe_resource`, `get_container_logs`, `get_recent_events`, `get_node_status`, `get_service_endpoints`) and their normalization layer.

Implemented starting Week 2 — see `docs/architecture.md` §4-5 (tool design philosophy, read-only safety boundary).
