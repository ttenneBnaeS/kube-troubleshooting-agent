# Backend API base URL. Defaults to http://localhost:8000 if unset, so this
# file only needs a real value when the backend isn't on the default port/host.
# NEXT_PUBLIC_API_URL=http://localhost:8000
